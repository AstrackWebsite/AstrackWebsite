[build]
  command = "npm run build"
  publish = ".next"

# Official Next.js runtime for Netlify — handles App Router, server
# components, and route handlers. Auto-installed by Netlify on detection,
# pinned here for reproducible builds.
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "22"
